'use strict'

// Alerta
alert("Esto es una alerta!!");


// Alerta de confirmación
var resultado = confirm("¿Estas seguro de querer continuar?");
console.log(resultado);

// Ingres de datos

var datos = prompt("¿Qué edad tienes?", 18);
console.log( parseInt(datos) );
